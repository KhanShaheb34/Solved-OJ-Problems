#include<bits/stdc++.h>
using namespace std;
#define ll long long

void solve(ll cs) {
    ll n;
    cin >> n;
    cout << ((n+1)/2) - 1 << endl;
}

int main() {
    ll t;
    cin >> t;
    for(ll i = 0; i < t; i++) {
        solve(i+1);
    }
    return 0;
}
