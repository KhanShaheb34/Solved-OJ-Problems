#include<iostream>
using namespace std;

int gcd(int a, int b)
{
    if (b == 0) return a;
    return gcd(b, a % b);
}

int main()[
    int a, b, n, m, f=0;
    cin>>n>>a>>b;
    m = gcd()
    n--;
    while(n--){
        cin>>b;
        if()
    }
    return 0;
]
