#include<bits/stdc++.h>
using namespace std;
#define ll long long
 
void solve(ll cs) {
    ll a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << b << " " << c << " " << c << endl;
}
 
int main() {
    ll t;
    cin >> t;
    for(ll i = 0; i < t; i++) {
        solve(i+1);
    }
    return 0;
}
